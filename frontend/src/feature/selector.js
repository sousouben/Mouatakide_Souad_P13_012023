/**
 * @function selectUser
 * @description Sélectionne l'utilisateur depuis l'état de l'application.
 * @param {object} state - L'état de l'application.
 * @returns {object} state.user - L'objet utilisateur.
 */
export const selectUser = (state) => state.user;
